# Vercel / локально для serverless api/order.ts (не префикс VITE — не попадает в клиент)
TELEGRAM_BOT_TOKEN=
# ID чата или канала, куда бот шлёт заявки (узнать у @userinfobot / @getidsbot)
TELEGRAM_CHAT_ID=

# Опционально: якщо фронт на GitHub Pages, а API на Vercel — повний URL
# VITE_ORDER_API_URL=https://your-project.vercel.app/api/order
REQUIRE_TELEGRAM_INIT_DATA=
TELEGRAM_INITDATA_MAX_AGE_SEC=
ORDER_LIMIT_PER_TG_USER_PER_DAY=